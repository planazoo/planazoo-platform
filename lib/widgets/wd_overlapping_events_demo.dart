import 'package:flutter/material.dart';
import 'package:unp_calendario/features/calendar/domain/models/overlapping_event_group.dart';

/// Widget de demostración que muestra eventos solapados
class OverlappingEventsDemo extends StatelessWidget {
  const OverlappingEventsDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Overlapping Events Demo'),
      ),
      body: const Center(
        child: Text('Overlapping Events Demo - Under construction'),
      ),
    );
  }
}